{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile and run",
      "type": "shell",
      "command": "${workspaceFolder}/batch/compile_and_run.bat"
    },
    {
      "label": "git fast checkpoint",
      "type": "shell",
      "command": "${workspaceFolder}/batch/git_fast_checkpoint.bat"
    }
  ]
}
